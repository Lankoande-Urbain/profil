{
   "builds": [
      {
         "name": "portofolio",
         "src": "/package.json",
         "use": "@vercel/static-build",
         "config": {
            "distDir": "dist",
            "entrypoint": "npm run build"
         }
      }
   ],
   "routes": [
      {
         "src": "/(.*)",
         "dest": "/index.html"
      }
   ]
}